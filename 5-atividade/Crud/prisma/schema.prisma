generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}


model User {
  Id      Int     @id @default(autoincrement())
  email   String  @unique
  nome    String
  idade   Int
  estado  String
  cidade  String
}

